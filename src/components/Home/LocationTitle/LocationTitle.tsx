import React from 'react'

import styles from './LocationTitle.module.scss'

const LocationTitle: React.FC = () => {
  return <h2 className={styles.root}>Stays in Finland</h2>
}

export default LocationTitle
